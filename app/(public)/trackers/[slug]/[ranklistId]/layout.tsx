export const metadata = {
  title: 'Tracker Details',
  description: 'View tracker rankings and events',
}

export default function TrackerLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return children;
}