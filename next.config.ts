import type { NextConfig } from "next";

const nextConfig: NextConfig = {images: {
        domains: ['lh3.googleusercontent.com', 'sgp1.digitaloceanspaces.com', 'cdn.diuacm.com'],
    },
};

export default nextConfig;
